services:
  db:
    image: nouchka/sqlite3:latest
    volumes:
      - db_data:/var/lib/sqlite/dairy_db.sql
    restart: on-failure
    env_file:
      - ./.env

  bot:
    build: .
    command: sh -c "python -m main"
    env_file:
      - ./.env
    restart: always
    volumes:
      - ./:/app/
    depends_on:
      - db

volumes:
  db_data:
